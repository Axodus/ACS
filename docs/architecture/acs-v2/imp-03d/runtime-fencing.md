# Runtime fencing

Assignment generation answers who coordination selected. The existing lease and fencing token answer which runtime holder may mutate an Attempt. IMP-03D does not merge these fields or create a second lease system.

Existing fenced checkpoint and runtime write paths remain responsible for rejecting stale workers.
