# Runtime projection

`GET /api/v1/tasks/:id/runtime?runId=...` exposes execution intents and sanitized Attempts with assignment generation, member slot, Agent revision and Workforce revision. Fencing tokens and lease secrets are omitted.
