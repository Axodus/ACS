# Assignment projection

Current assignment comes from `getCurrentTaskAssignment`. History comes from `listTaskAssignments`. Attempts are never used to infer assignment, and reassignment history remains append-only.
