# PostgreSQL validation

The implementation uses the migration 5–7 canonical tables and adds no migration. PostgreSQL validation must cover initialization, Workforce history, admission membership, coordination lineage, runtime binding and legacy Run compatibility.
