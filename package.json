{
  "name": "@axodus/acs-core",
  "version": "0.1.0",
  "description": "Axodus Cognitive Systems core runtime contracts and deterministic orchestration primitives.",
  "type": "module",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test tests/*.test.mjs",
    "check": "npm run test",
    "acs": "npm run build && node scripts/acs.mjs",
    "smoke:openclaw": "npm run build && node scripts/smoke-openclaw.mjs",
    "smoke:runtime": "npm run build && node scripts/smoke-runtime.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3"
  }
}
